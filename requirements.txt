# Core image-generation stack (CPU-friendly defaults)
torch==2.2.2
torchvision==0.17.2
diffusers==0.27.2
transformers==4.40.2
accelerate==0.30.1
safetensors==0.4.3
Pillow==10.3.0

# UI
gradio==4.31.5

# Utilities
numpy==1.26.4
python-dateutil==2.9.0.post0

# Optional AMD/DirectML acceleration on Windows.
# Install separately if you want GPU acceleration on Radeon:
#   py -m pip install torch-directml
